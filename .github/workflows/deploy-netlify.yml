name: Deploy to Netlify

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify Deploy
        run: |
          curl -X POST https://api.netlify.com/build_hooks/2e637edc-3abd-4f00-8584-23d32925b3f4
